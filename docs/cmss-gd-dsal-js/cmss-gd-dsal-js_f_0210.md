## 第5章

这些是位于[​`Exercises`​](f_0055.xhtml#optimizing.code.with.and.without.big.o.exercises)部分的练习的解答。

1.  在去掉常数后，我们可以将表达式简化为`O(N)`。

1.  在去掉常数后，我们可以将表达式简化为`O(N²)`。

1.  该算法的复杂度为`O(N)`，其中`N`是数组的大小。虽然有两个独立的循环处理`N`个元素，但这仅仅是`2N`，在去掉常数后简化为`O(N)`。

1.  该算法的复杂度为`O(N)`，其中`N`是数组的大小。在循环中，我们执行三步，这意味着我们的算法需要`3N`步。然而，当我们去掉常数时，这被简化为`O(N)`。

1.  该算法的复杂度为`O(N²)`，其中`N`是数组的大小。虽然我们仅运行内部循环一半的时间，但这仅意味着算法运行了`N² / 2`步。然而，除以2是一个常数，因此我们简单地将其表示为`O(N²)`。
