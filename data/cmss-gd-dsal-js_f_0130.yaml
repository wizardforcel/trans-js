- en: Chapter 14
  id: totrans-0
  prefs:
  - PREF_H1
  stylish: true
  type: TYPE_NORMAL
  zh: 第14章
- en: '`Node`-Based Data Structures'
  id: totrans-1
  prefs: []
  stylish: true
  type: TYPE_NORMAL
  zh: '`节点`基础数据结构'
- en: For the next several chapters, we’re going to explore a variety of data structures
    that all build upon a single concept—the `node`. As you’ll see shortly, `nodes`
    are pieces of data that may be dispersed throughout the computer’s memory. `Node`-based
    data structures offer new ways to organize and access data that provide a number
    of major performance advantages.
  id: totrans-2
  prefs: []
  stylish: true
  type: TYPE_NORMAL
  zh: 在接下来的几章中，我们将探索多种数据结构，它们都基于一个单一概念——`节点`。正如你将很快看到的，`节点`是分散在计算机内存中的数据片段。`节点`基础数据结构提供了组织和访问数据的新方法，从而带来了许多主要的性能优势。
- en: In this chapter we’ll explore the `linked list`, which is the simplest `node`-based
    data structure and the foundation for future chapters. You’ll discover that `linked
    lists` seem almost identical to `arrays` but come with their own set of trade-offs
    in efficiency that can give us a performance boost for certain situations.
  id: totrans-3
  prefs: []
  stylish: true
  type: TYPE_NORMAL
  zh: 在这一章中，我们将探讨`链表`，这是最简单的`节点`基础数据结构，也是后续章节的基础。你会发现`链表`与`数组`看起来几乎相同，但在效率上有自己的一套权衡，这可以在某些情况下为我们提供性能提升。
